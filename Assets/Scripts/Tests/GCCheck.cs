using UnityEngine;

public class GCCheck : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GCNotifier.Collected += () => Debug.Log("collected");
        //GCNotifier.Start();
    }
}
